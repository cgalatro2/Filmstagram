<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>MovieBlog</title>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
		<link rel="stylesheet" href="/stylesheets/main.css">
		<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
    </head>
	<body>
		
		<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
			<i class="fas fa-film fa-2x"></i>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    			<span class="navbar-toggler-icon"></span>
  			</button>

			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav narbar-right mr-auto">
					<li class="nav-item active">
						<a class="navbar-brand" href="/movies">MovieBlog</a>
					</li>
				</ul>
				<span class="navbar-text">
					<ul class="navbar-nav narbar-right mr-auto">
						<% if (!currentUser) { %>
							<li class="nav-item">
								<a  class="nav-link <%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>" href='/login'>Login </a>
							</li>
							<li class="nav-item">
								<a  class="nav-link <%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>" href='/register'>Sign Up</a>
							</li>
						<% } else { %>
							<li class="nav-item">
								<a  class="nav-link" href='/logout'>Logout</a>
							</li>
						<% } %>
					</ul>
				</span>
			</div>
			
		</nav>